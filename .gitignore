# Environment — NEVER commit your API key
.env

# Personal launcher (use launch-vox-refiner.example.sh as template)
launch-vox-refiner.sh
# Legacy personal launcher name (kept ignored for backward compatibility)
launch_vox-refiner.sh

# Auto-generated history context (personal, stays local)
history.txt

# Personal context (copy from context.example.txt)
context.txt

# Audio files (temporary, generated at runtime)
*.wav
*.mp3

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# OS
.DS_Store
Thumbs.db
.directory
